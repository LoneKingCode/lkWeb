导入 未实现 导入类型：更新， 插入  还有外键。。。。等。
Enum 列 未测试

表结构管理 扩展 按钮 查看表数据 对于列的 各个属性扩展功能 实现
  注意: 查看 导入 导出等 顺序 未实现


执行sql delete语句 不会返回影响行数

             var delResult = await _roleMenuService.Delete(item => item.RoleId == roleId);
                if (!delResult.flag)
                    result.msg += delResult.msg + "\n";
				role 第一次设置菜单权限 返回信息不正确。


数据库 sqlService  EF  事务性   + nlog 日志输出

输入框 加入输入提示 对于表结构管理的详细设置说明

字段保存类型 默认的string是nvarchar(max) 用哪个？varchar or nvarchar
数据库 初始化代码 初始 数据 并且测试

调整startup.cs 里面太乱

菜单 自适应缩放

登陆时根据模块。。。

图表统计

注意lkWebDbContextFactory 里写的明文 SQL链接

hangfire 定时任务
* datatable 排序 后台暂时写死不是根据前台参数修改的 所有columns定义的 里面多加个Name.